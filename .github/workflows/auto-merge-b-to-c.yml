name: Auto Merge B to C

on:
  workflow_dispatch:
  push:
    branches:
      - test-env-b

jobs:
  auto-merge:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      PR_TITLE: "[Conflict] test-env-b -> test-env-c"
      TARGET_BRANCH: test-env-b
      BASE_BRANCH: test-env-c

    steps:
      - name: Checkout branchA
        uses: actions/checkout@v4
        with:
          ref: ${{ env.TARGET_BRANCH }}
          fetch-depth: 0

      - name: Fetch branchB
        run: git fetch origin branchB

      - name: Check if branchB is merged
        id: check_source
        run: |
          if git merge-base --is-ancestor origin/${{ env.BASE_BRANCH }} HEAD; then
            echo "merged_base=true" >> $GITHUB_OUTPUT
          else
            echo "merged_base=false" >> $GITHUB_OUTPUT
          fi

      - name: Checkout base branch (test-env-b)
        if: steps.check_source.outputs.merged_base == 'false'
        uses: actions/checkout@v4
        with:
          ref: ${{ env.BASE_BRANCH }}
          fetch-depth: 0

      - name: Set up Git
        if: steps.check_source.outputs.merged_base == 'false'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Fetch source branch (test-env-b)
        if: steps.check_source.outputs.merged_base == 'false'
        run: |
          git fetch origin test-env-b:test-env-b

      - name: Check for merge conflicts
        if: steps.check_source.outputs.merged_base == 'false'
        id: merge_check
        run: |
          echo "🔍 Checking for conflicts..."
          if git merge --no-commit --no-ff test-env-b; then
            echo "has_conflict=false" >> "$GITHUB_OUTPUT"
          else
            echo "has_conflict=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Reset merge state
        if: steps.check_source.outputs.merged_base == 'false'
        run: git reset --merge || true

      - name: Auto-merge if no conflict
        if:  steps.check_source.outputs.merged_base == 'false'
          && steps.merge_check.outputs.has_conflict == 'false'
        run: |
          echo "🚀 Performing auto-merge and pushing to ${{ env.BASE_BRANCH }}"
          git merge --no-edit test-env-b
          git push origin ${{ env.BASE_BRANCH }}

      - name: Create conflict resolution branch and PR
        if:  steps.check_source.outputs.merged_base == 'false'
          && steps.merge_check.outputs.has_conflict == 'true'
        run: |
          BRANCH_NAME=auto/conflict-fix-$(date +%s)
          git checkout -b $BRANCH_NAME origin/test-env-b
          git push origin $BRANCH_NAME

          echo "🔧 Creating PR for manual conflict resolution"
          gh pr create \
            --base ${{ env.BASE_BRANCH }} \
            --head $BRANCH_NAME \
            --title "${{ env.PR_TITLE }}" \
            --reviewer yannsugi \
            --body "Auto-generated PR due to merge conflict between test-env-b and test-env-b."
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
