name: Merge or Pull Request A to B

on:
  push:
    branches:
      - test-env-a

jobs:
  merge-or-pull-request-a-to-b:
    uses: ./.github/workflows/merge-or-pull-request.yml
    with:
      head_branch: test-env-a
      base_branch: test-env-b
      next_workflow: merge-or-pull-request-b-to-c.yml
